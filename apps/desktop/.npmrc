# @typescript-eslint/* still declares peer typescript <6 until a release widens the range;
# TypeScript 6 works in practice — see https://typescript-eslint.io/users/dependency-versions/
legacy-peer-deps=true
