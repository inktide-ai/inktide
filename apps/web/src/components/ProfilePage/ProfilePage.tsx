/**
 * Re-export: authenticated shell with sidebar now lives in `layouts/ProfileShell`.
 * Kept so imports like `import('./components/ProfilePage')` or git history on this path stay valid.
 */
export { default } from '../../layouts/ProfileShell/ProfileShell'
