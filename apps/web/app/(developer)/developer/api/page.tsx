import type { Metadata } from 'next'
import { ApiPage } from '@/features/developer/docs/api-page'

export const metadata: Metadata = {
  title: 'REST API — Inktide Docs',
  description: 'REST API reference with endpoint table, authentication, and TTS schema.',
}

export default ApiPage
