import type { Metadata } from 'next'
import { QuickStartPage } from '@/features/developer/docs/quickstart-page'

export const metadata: Metadata = {
  title: 'Quick Start — Inktide Docs',
  description: 'Run the full Inktide stack locally in five steps.',
}

export default QuickStartPage
