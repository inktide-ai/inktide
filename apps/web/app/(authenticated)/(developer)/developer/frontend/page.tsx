import type { Metadata } from 'next'
import { FrontendPage } from '@/features/developer/docs/frontend-page'

export const metadata: Metadata = {
  title: 'Frontend Architecture — Inktide Docs',
  description: 'FSD layers, TanStack Query, Keycloak auth flow, character model, and API proxy.',
}

export default FrontendPage
