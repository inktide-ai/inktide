import type { Metadata } from 'next'
import { RoutesPage } from '@/features/developer/docs/routes-page'

export const metadata: Metadata = {
  title: 'Frontend Routes — Inktide Docs',
  description: 'All Next.js App Router routes — workspace and public pages.',
}

export default RoutesPage
