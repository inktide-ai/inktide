import type { Metadata } from 'next'
import { ArchitecturePage } from '@/features/developer/docs/architecture-page'

export const metadata: Metadata = {
  title: 'Architecture — Inktide Docs',
  description: 'Message pipeline, bounded contexts, and dependency rules for the Inktide platform.',
}

export default ArchitecturePage
