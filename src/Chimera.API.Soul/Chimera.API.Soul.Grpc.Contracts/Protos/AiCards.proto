syntax = "proto3";

option csharp_namespace = "Chimera.API.Soul.Grpc.Contracts.AiCards";

package chimera.soul;

// ─────────────────────────────────────────────────────────────────────────────
// AiCard Service — internal gRPC API for AI companion card management.
// Consumed by other backend services (e.g. AI.Oracle, Donkey Engine) that
// need to read card configuration without going through the public REST API.
// ─────────────────────────────────────────────────────────────────────────────

service AiCardService {
  // Returns a single AI card by its ID (for the given owner).
  rpc GetCard (GetCardRequest) returns (AiCardResponse);

  // Returns all AI cards owned by a user.
  rpc ListCards (ListCardsRequest) returns (ListCardsResponse);

  // Creates a new AI card.
  rpc CreateCard (CreateCardRequest) returns (AiCardResponse);

  // Updates an existing AI card (full replace of mutable fields).
  rpc UpdateCard (UpdateCardRequest) returns (AiCardResponse);

  // Soft-deletes an AI card.
  rpc DeleteCard (DeleteCardRequest) returns (DeleteCardResponse);
}

// ─── Requests ────────────────────────────────────────────────────────────────

message GetCardRequest {
  string user_id = 1;
  string card_id = 2;
}

message ListCardsRequest {
  string user_id = 1;
}

message CreateCardRequest {
  string user_id        = 1;
  string name           = 2;
  string slug           = 3;
  string personality    = 4;
  string system_prompt  = 5;
  string llm_catalog_id = 6;
  string llm_config     = 7;  // JSON blob
  optional string tts_catalog_id = 8;
  optional string tts_config     = 9;  // JSON blob
  string behavior        = 10; // JSON blob
  string memory_settings = 11; // JSON blob
  string donkey_engine   = 12; // JSON blob
  optional string avatar_url = 13;
}

message UpdateCardRequest {
  string user_id        = 1;
  string card_id        = 2;
  string name           = 3;
  string slug           = 4;
  string personality    = 5;
  string system_prompt  = 6;
  string llm_catalog_id = 7;
  string llm_config     = 8;
  optional string tts_catalog_id = 9;
  optional string tts_config     = 10;
  string behavior        = 11;
  string memory_settings = 12;
  string donkey_engine   = 13;
  bool   is_active       = 14;
  optional string avatar_url = 15;
}

message DeleteCardRequest {
  string user_id = 1;
  string card_id = 2;
}

// ─── Responses ───────────────────────────────────────────────────────────────

message AiCardResponse {
  string card_id        = 1;
  string user_id        = 2;
  string name           = 3;
  string slug           = 4;
  string personality    = 5;
  string system_prompt  = 6;
  string llm_catalog_id = 7;
  string llm_config     = 8;
  optional string tts_catalog_id = 9;
  optional string tts_config     = 10;
  string behavior        = 11;
  string memory_settings = 12;
  string donkey_engine   = 13;
  bool   is_active       = 14;
  string created_at      = 15; // ISO-8601
  string updated_at      = 16; // ISO-8601
  optional string avatar_url = 17;
}

message ListCardsResponse {
  repeated AiCardResponse cards = 1;
}

message DeleteCardResponse {
  bool success = 1;
}
